spring:
    application:
        name: SpringBoot
    datasource:
        password: postgresql
        url: jdbc:postgresql://localhost:5432/spring
        username: postgres
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
server:
    port: 8080